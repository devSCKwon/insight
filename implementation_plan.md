# 그룹웨어 미결업무 연속성 교육자료 제작 계획서

본 계획서는 `그룹웨어내 미결업무 연속성 개선 방안 검토.md` 내용을 바탕으로, 기존 샘플 `security_edu_20260401.html` 파일의 레이아웃과 스타일을 유지한 채 새로운 사용자 교육자료 HTML을 제작하기 위한 세부 계획안입니다.

## 사용자 검토 요구사항
- 새로 생성할 폴더명: `20260625_gw_continuity_edu`
- 생성할 파일명: `gw_continuity_edu.html`
- 본 자료는 시스템 관리자가 아닌 **일반 실무 사용자**가 파워쿼리 기반 자동화 도구를 쉽게 활용할 수 있도록 돕는 데 초점을 맞춥니다.
- 이미지 파일은 추후 실제 캡처본으로 대체할 수 있도록 **상세한 설명(alt)을 포함한 빈 이미지 영역**으로 배치합니다.

---

## 제안하는 교육자료 구성안

### 1. 화면 레이아웃 및 스타일 (기존 서식 유지)
- **사이드바 내비게이션**:
  - `시스템 개요` (`#system-overview`, 아이콘: `fas fa-info-circle`)
  - `실무 사용 3단계 가이드` (`#user-guide`, 아이콘: `fas fa-tasks`)
  - `핵심 기능 및 개선 효과` (`#system-features`, 아이콘: `fas fa-chart-line`)
  - `실행 체크리스트 & FAQ` (`#checklist-faq`, font-awesome 아이콘: `fas fa-check-double`)
- **디자인 테마**: 전문적인 비즈니스 툴의 느낌을 주기 위해 기존 신뢰감 있는 블루 톤(`--primary-color: #0052cc`, `--header-bg-color: #0747a6`)을 유지하며, 가독성이 뛰어난 `Paperlogy` 폰트를 사용합니다.

### 2. 콘텐츠 상세 기획

#### [섹션 1] 시스템 개요 및 구축 현황 (`#system-overview`)
- **내용**: 그룹웨어 결재문서(원가 외주 가격결정품의서, 구매 가격결정품의서 등)의 원본 수정 제약을 극복하고 수작업을 제로화하기 위해 도입된 '파워쿼리 자동화 시스템'을 소개합니다.
- **포함 요소**:
  - 시스템 개요 카드
  - 프로그램 추가 개발 완료 현황 카드 (100% 완료 상태 강조)
  - **이미지 배치 1**: `image/continuity_system_map.png`
    - *alt 설명*: "그룹웨어 결재문서 데이터가 지정 폴더를 거쳐 파워쿼리로 병합되고 미결관리 마스터 엑셀에 자동으로 반영되는 전체 데이터 흐름도"

#### [섹션 2] 실무 사용 3단계 가이드 (`#user-guide`)
- **내용**: 실무자가 매일 수행해야 하는 직관적인 3단계 프로세스를 카드 형식으로 단계별 이미지와 함께 제공합니다.
- **단계별 내용**:
  - **1단계: 그룹웨어 다운로드 및 파일 저장**
    - 그룹웨어 결재문서 리포트 조회 후 다운로드하여 지정 폴더(`업무공용폴더:\가격결정품의_관리`)에 파일명 수정 없이 그대로 저장
    - **이미지 배치 2**: `image/gw_download_save.png`
      - *alt 설명*: "그룹웨어에서 가격결정 결재문서 리포트를 조회하여 다운로드한 후, 업무공용폴더 내 지정된 경로에 저장하는 화면"
  - **2단계: 마스터 파일 새로고침 실행**
    - `가격결정품의_미결관리마스터.xlsx` 실행 ➔ `[데이터] -> [모두 새로고침]` 클릭 또는 단축키 `Ctrl + Alt + F5` 입력
    - **이미지 배치 3**: `image/master_refresh.png`
      - *alt 설명*: "미결관리마스터 엑셀 파일 내에서 '모두 새로고침' 버튼 또는 단축키(Ctrl+Alt+F5)를 작동시키는 예시 화면"
  - **3단계: 미결 확인 및 조치내역 입력**
    - 하단에 자동 추가된 **`★미결(조치필요)`** 상태의 행들을 확인하고, 우측 수동 입력 열에 `[진행 상태]`, `[실제 반영일]`, `[조치 내용 로그]` 수동 입력
    - **이미지 배치 4**: `image/action_logging.png`
      - *alt 설명*: "새로고침 후 자동으로 자석 매칭된 문서번호 기준 행에서 미결 건을 확인하고 조치 기록을 우측에 타이틀 그대로 입력하는 마스터 시트 예시"

#### [섹션 3] 핵심 기능 및 기대 효과 (`#system-features`)
- **내용**: 시스템이 제공하는 기술적 편리함과 도입 후 누릴 수 있는 정량적/정성적 효과를 정리합니다.
- **핵심 기능**:
  - **파워쿼리 자동 정제**: 지정 폴더 내 모든 파일 자동 병합, 중복 데이터 최신일 기준 자동 제거
  - **행 밀림 방지(자석 매칭)**: 사용자가 입력한 조치 내역이 행 순서가 바뀌어도 고유 '문서번호'를 기준으로 고정 매칭
  - **선제적 모니터링 경보**: 7일 이상 지연 시 **노란색**, 14일 이상 지연 시 **빨간색**으로 지연일 자동 시각화 경고 제공
- **기대 효과**: 수작업 제로화, 이력 영구 보존, 마감 지연 사전 방지

#### [섹션 4] 실행 체크리스트 & FAQ (`#checklist-faq`)
- **체크리스트**: 사용자가 업무 시작 전에 반드시 체크해야 할 액션 리스트
- **Q&A**:
  - Q: 과거 파일을 지우고 새로 다운로드한 파일만 저장해야 하나요? (A: 지우지 않고 계속 쌓아두어도 파워쿼리가 최신 작성일 기준으로 중복을 알아서 정제해 줍니다.)
  - Q: 조치 내역을 입력했는데 엑셀 정렬을 바꾸면 데이터가 밀리지 않나요? (A: 고유 문서번호 기준 자석 매칭 로직이 적용되어 밀리지 않고 안전하게 보존됩니다.)

---

## Proposed Changes

### [20260625_gw_continuity_edu]

#### [NEW] [gw_continuity_edu.html](file:///d:/00_Source/devSCKwon/insight/20260625_gw_continuity_edu/gw_continuity_edu.html)
- 기존 `security_edu_20260401.html`의 CSS 스타일, 사이드바 반응형 스크립트, 모달 스크립트를 재활용합니다.
- 마크다운 내용에 기반하여 본문 텍스트와 UI 요소를 재배치합니다.
- 이미지는 `image/` 상대 경로를 사용하되 상세한 `alt`와 가이드 영역을 렌더링합니다.

---

## 검토 요청 사항
1. 상기 제안된 **4개 섹션 구성안**이 교육을 배포하기에 적합한지 검토 부탁드립니다.
2. 추가로 다루었으면 하는 내용이나, 수정해야 할 흐름이 있다면 의견을 남겨주시면 최종 구현 시 적극 반영하겠습니다.
